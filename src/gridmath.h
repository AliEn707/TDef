#include <math.h>
#include <stdio.h>
void initGridMath();

float gcos(float a);

float gsin(float a);

//float gsqrt(float a);

float glength(vec* v1,vec* v2);

float getDir(vec* v1,vec* v2, vec* out);

